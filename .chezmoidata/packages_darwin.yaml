# macOS-only packages. Merged with packages_base by the install script when
# .chezmoi.os == "darwin". All keys kept present (even empty) because the config
# sets template option missingkey=error.
packages:
  darwin:
    system:
      - coreutils
    brew:
      light:
        # keg-only CLI that moves files to the macOS Trash (not Finder "rm").
        # PATH is extended in dot_zsh/bootstrap.zsh.tmpl via brew --prefix trash.
        - trash
      full: []
      casks:
        - ghostty
        - font-jetbrains-mono-nerd-font
